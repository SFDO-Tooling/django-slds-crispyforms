============
Installation
============

At the command line::

    $ easy_install django-slds-crispyforms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-slds-crispyforms
    $ pip install django-slds-crispyforms
